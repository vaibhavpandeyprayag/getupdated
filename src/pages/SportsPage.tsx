import { FC, memo } from "react";

interface Props {}

const SportsPage: FC<Props> = (props) => {
  return <div className=""></div>;
};

SportsPage.defaultProps = {};

export default memo(SportsPage);
